[build]
builder = "dockerfile"
buildCommand = "composer install --no-dev --optimize-autoloader && php artisan config:clear && php artisan cache:clear && php artisan view:clear && chmod -R 775 storage bootstrap/cache"

[deploy]
healthcheckPath = "/health"